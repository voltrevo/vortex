x := 1; // #info #is-duplicated

for (x := 1; y; x++) { // #error #error #error #duplicate #not-found #invalid-condition #for-loop
  y := 1;
  return y;
}

return y; // #error #not-found TODO:unreachable
