// import Error from 'core'; // TODO:error:not-implemented

return func factorial(n: i32) => switch { // TODO:info:return-value
  (n < 0) => Error('Invalid input'); // #error #not-found
  (n == 0) => 1;
  (true) => n * factorial(n - 1);
};
