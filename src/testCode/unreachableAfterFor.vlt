func foo() { // #warning
  sum := 0;

  i := 0;

  for {
    sum += i;
    i++;

    if (i == 10) {
      return sum;
    }
  }

  return sum; // TODO this is unreachable
};

return foo(); // #error TODO: need to lift scope of foo
