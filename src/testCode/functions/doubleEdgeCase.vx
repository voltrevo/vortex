assert false; // Validation should not pass

sum := 0;

if (true) {
  a := 1;
  sum += a;
  sum += outer(); // #error closure incomplete
}

a := 100;

func outer() {
  func inner() {
    return a;
  };

  return inner();
};

return sum;
