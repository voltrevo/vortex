func factorial(n) {
  if (n == 0) {
    return 1; // #info #return-value
  }

  // TODO: Suppress excessive return-value notes
  return n * factorial(n - 1); // #info #return-value #info #info #info #info
};

assert factorial(5) == 120;

return 'done'; // #info #return-value
