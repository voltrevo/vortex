x := 1; // #warn #unused

func() {
  y := 1; // #warn #unused #mutation
  y += 1;
  z := 1; // #warn #unused
  z++;
  zz := 1; // #warn #unused
  a := (zz += 1); // #error #subexpression-mutation
  return a;
};

return 1;
