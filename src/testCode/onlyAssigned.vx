x := 1; // #warning #unused

func() {
  y := 1; // #warning #unused only assigned
  y += 1;
  z := 1; // TODO should be warning ... hmm how to handle this one?
  z++;
  zz := 1; // #warning #unused TODO shouldn't be a warning (similar to ++ issue)
  a := (zz += 1); // #error #subexpression-mutation
  return a;
};

return 1;
