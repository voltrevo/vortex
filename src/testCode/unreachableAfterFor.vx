func foo() {
  sum := 0;

  i := 0;

  for {
    sum += i;
    i++;

    if (i == 10) {
      return sum; // #info #return-value
    }
  }

  return sum; // TODO:unreachable
};

return foo(); // #info #return-value
