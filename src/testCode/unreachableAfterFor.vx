func foo() {
  sum := 0;

  i := 0;

  for {
    sum += i;
    i++;

    if (i == 10) {
      return sum;
    }
  }

  return sum; // TODO-easy:unreachable (implemented in bootstrapped version)
};

return foo();
