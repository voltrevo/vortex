return func first(arr, len, before) { // #warn #unused TODO: not unused
  result := arr[0];

  for (i := 1; i < len; i++) {
    el := arr[i];

    if (before(el, result)) {
      result = el;
    }
  }

  return result;
};
