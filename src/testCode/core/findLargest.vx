return func findLargest(arr, len, lessThan) { // #info #return-value #warn #unused TODO: not unused
  largest := arr[0];

  for (i := 1; i < len; i++) {
    el := arr[i];

    if (lessThan(largest, el)) {
      largest = el;
    }
  }

  return largest;
};
