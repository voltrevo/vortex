import @/CommaSequence.vx;
import p from '@/parse.vx';

import ./DeferLiteral.vx;

RawArray := p.Sequence([
  p.Exact('['),
  CommaSequence(DeferLiteral),
  p.Exact(']'),
]);

Array := p.Transform(
  RawArray,
  func([start, value, end]) => ['literal', value],
);

return Array;
