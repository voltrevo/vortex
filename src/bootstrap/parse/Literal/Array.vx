import @/parse/CommaSequence.vx;
import p from '@/parse/parse.vx';

import ./DeferLiteral.vx;

RawArray := p.SequenceOne([
  [p.Exact('[')],
  CommaSequence(DeferLiteral),
  [p.Exact(']')],
]);

Array := p.Transform(
  RawArray,
  func(value) => ['literal', value],
);

return Array;
