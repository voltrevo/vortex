assert [] == [];
assert [1] == [1];

arrs := [
  [0, 0],
  [0, 1],
  [1, 0],
  [1, 1],
];

for (i := 0; i < 4; i++) {
  for (j := 0; j < 4; j++) {
    assert (arrs[i] == arrs[j]) == (i == j);
    assert (arrs[i] != arrs[j]) == (i != j);
    assert (arrs[i] < arrs[j]) == (i < j);
    assert (arrs[i] > arrs[j]) == (i > j);
    assert (arrs[i] <= arrs[j]) == (i <= j);
    assert (arrs[i] >= arrs[j]) == (i >= j);
  }
}

assert [1, [2, 3]] == [1, [2, 3]];
assert [1, [3, 2]] > [1, [2, 3]];

return 0; // #info
