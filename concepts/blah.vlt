foo := {
  a: 10,
  b: 2,
};

foo.a = x := 2; // error
foo.a = (x = (y := 2)); // error (creation just more deeply nested)
foo.a = (x.y := 2); // ok: just adding prop, not creating variable
foo.c := 3;
foo.d := {};

func() { // TODO top level anonymous function can't be called
  return a := 1; // error
};

foo.d.e := 42; // error
