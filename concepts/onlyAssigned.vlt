x := 1; // warning unused

func() {
  y := 1; // warning only assigned
  y += 1;
  z := 1; // warning... hmm how to handle this one?
  z++;
  zz := 1; // shouldn't be a (w)arning (similar to ++ issue)
  a := (zz += 1);
  return a;
};

return 1;
